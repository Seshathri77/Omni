---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: expense-service
  namespace: yath
spec:
  replicas: 1
  selector:
    matchLabels:
      app: expense-service
  template:
    metadata:
      labels:
        app: expense-service
    spec:
      imagePullSecrets:
      - name: acr-secret
      containers:
      - name: expense-service
        image: yathregistry.azurecr.io/yath-expense-service:latest
        ports:
        - containerPort: 80
          name: http
        env:
        - name: ASPNETCORE_ENVIRONMENT
          value: "Production"
        - name: ASPNETCORE_URLS
          value: "http://+:80"
        resources:
          requests:
            memory: "512Mi"
            cpu: "250m"
          limits:
            memory: "1Gi"
            cpu: "500m"
---
apiVersion: v1
kind: Service
metadata:
  name: expense-service
  namespace: yath
spec:
  selector:
    app: expense-service
  ports:
  - port: 80
    targetPort: 80
  type: LoadBalancer
