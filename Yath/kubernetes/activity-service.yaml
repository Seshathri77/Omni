---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: activity-service
  namespace: yath
spec:
  replicas: 1
  selector:
    matchLabels:
      app: activity-service
  template:
    metadata:
      labels:
        app: activity-service
    spec:
      imagePullSecrets:
      - name: acr-secret
      containers:
      - name: activity-service
        image: yathregistry.azurecr.io/yath-activity-service:latest
        ports:
        - containerPort: 80
          name: http
        env:
        - name: ASPNETCORE_ENVIRONMENT
          value: "Production"
        - name: ASPNETCORE_URLS
          value: "http://+:80"
        resources:
          requests:
            memory: "512Mi"
            cpu: "250m"
          limits:
            memory: "1Gi"
            cpu: "500m"
---
apiVersion: v1
kind: Service
metadata:
  name: activity-service
  namespace: yath
spec:
  selector:
    app: activity-service
  ports:
  - port: 80
    targetPort: 80
  type: LoadBalancer
